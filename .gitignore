# Xcode
ios/Ribbon.xcodeproj/xcuserdata/
ios/Ribbon.xcodeproj/project.xcworkspace/xcuserdata/
ios/Ribbon.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/
DerivedData/
*.xcuserstate

# Swift Package Manager
core/.build/
core/.swiftpm/

# Python
__pycache__/
*.pyc

# Secrets. The send-email hook's secret, the mail provider's key and the
# throttle pepper live in supabase/functions/.env on a developer's machine
# and are pushed with `supabase secrets set` — they must never be committed.
.env
.env.*
!.env.example

# Supabase CLI scratch
supabase/.temp/
supabase/.branches/

# OS
.DS_Store
